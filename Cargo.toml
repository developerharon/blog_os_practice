[package]
name = "blog_os"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.dev]
panic = "abort" # disable stack unwinding on panic

[profile.release]
panic = "abort" # disable stack unwinding on panic

[[bin]]
name = "blog_os"
test = false
bench = false